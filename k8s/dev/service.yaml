apiVersion: v1
kind: Service
metadata:
  name: dev
  namespace: calypso
spec:
  selector:
    app: dev
  ports:
    - name: ssh
      port: 22
      targetPort: 22
  # NodePort exposes SSH externally so developers and IDEs can attach.
  # The port is fixed to avoid configuration drift across sessions.
  type: NodePort
